import 'package:flutter/material.dart';

class Contarct extends StatelessWidget {
  const Contarct({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: const Text("Housing Contract"),
    ));
  }
}
